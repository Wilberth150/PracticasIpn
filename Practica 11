#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    double x, fx;

    printf("Enter a value for x: ");
    scanf("%lf", &x);

    fx = cos(x + 1) - sin(x + 1) + 1;

    printf("f(x) = %f\n", fx);

    return 0;
}

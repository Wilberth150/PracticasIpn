#include <stdio.h>
#include <math.h>

double f(double x) {
  return x - sin(x) + 3;
}

int main() {
  double x = 0.0;
  double resultado;

  printf("Da un valor a x: ");
  scanf("%lf", &x);

  resultado = f(x);

  printf("f(%.2lf) = %.2lf\n", x, resultado);

  return 0;
}
